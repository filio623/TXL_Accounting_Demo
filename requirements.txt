# Core dependencies
# To be filled in as we add dependencies 

# Core dependencies
pandas>=2.0.0  # For data processing
openpyxl>=3.1.0  # For Excel file handling
python-dotenv>=1.0.0  # For environment variables
pyyaml>=6.0.0  # For configuration files

# AI/ML dependencies (to be used later)
# openai>=1.0.0
# scikit-learn>=1.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0 